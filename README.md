# Multi-Agent Equation Processing System

This repository contains a two-agent system for processing and analyzing mathematical equations. The first agent generates an equation based on a provided template, and the second agent determines the type of the equation (quadratic, linear, or cubic) using predefined rules.

---

## **Action class: `action_translate_equation`**
Generates an equation based on a template and performs argument mapping if needed.

#### **Parameters:**
- `equationTemplateAddr`: Contains a description of the equation template.
- *(Optional)* `argumentsSet`: A set of arguments for variables in the equation.

#### **Workflow:**
1. The agent replaces placeholders in the equation template with values from the argument set.
2. The generated equation is passed to **Agent 2** for type determination.

#### **Example Input:**
- **Template:** `a*x^2 + b*x + c = 0`
- **Arguments:** `{a: 2, b: 4, c: -6}`

#### **Example Output:**
- **Generated Equation:** `2*x^2 + 4*x - 6 = 0`

---

### **Agent 2: `action_define_equation_type`**
Determines the type of the equation generated by **Agent 1** based on predefined rules.

#### **Parameters:**
- `equationAddr`: Contains the equation generated by **Agent 1**.
- *(Optional)* `ruleSet`: A set of rules for determining the type of the equation.

#### **Workflow:**

1. **Agent 1: `action_translate_equation`**
   - Receives a **URL** containing a mathematical expression.
   - Downloads or retrieves the expression from the provided link.
   - Translates the expression into a standard mathematical format based on a predefined template.
   - Passes the translated equation to **Agent 2** for further analysis.

2. **Agent 2: `action_define_equation_type`**
   - Receives the translated equation from **Agent 1**.
   - Analyzes the structure of the equation to determine its type:
     - **Linear**: Contains only terms up to the first degree (e.g., `x`).
     - **Quadratic**: Contains terms up to the second degree (e.g., `x^2`).
     - **Cubic**: Contains terms up to the third degree (e.g., `x^3`).
   - Returns the equation type along with the analyzed expression.

![Example Image](https://github.com/jakxcvv/jakxcvv1/blob/main/photo_2025-04-14_10-51-00.jpg)

#### **Example Input:**
![Example Image]([https://example.com/image.png](https://github.com/jakxcvv/jakxcvv1/blob/main/photo_2025-04-14_10-52-11.jpg))

#### **Example Output:**
![Example Image](https://github.com/jakxcvv/jakxcvv1/blob/main/photo_2025-04-14_10-52-11.jpg)

---

## **Result Codes**
Both agents use the following result codes:

| Code                             | Description                                   |
|----------------------------------|-----------------------------------------------|
| `SC_RESULT_LINEAR_EQUATION`      | Linear equation                               |
| `SC_RESULT_QUADRATIC_EQUATION`   | Quadratic equation                            |
| `SC_RESULT_CUBIC_EQUATION`       | Cubic equation                                |    
